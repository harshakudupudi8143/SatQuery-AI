{
  "name": "satquery-ai",
  "version": "1.0.0",
  "description": "SatQuery AI Fullstack App",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "npm start --prefix server"
  }
}
